module Vardec;

type A is record
    var a: integer;
    var b: real;
end;

type B is record
    var c: A;
    var d: string;
end;

routine ddd(val: integer): string is	
	if val > 0 then
		return "pos";
	end

	return "mmm";
end

routine gg(c: A, d: B) is
    Print("amogus");
    gg(c, d);
end

routine empty(s: integer): integer is
    IntToString(6);
	return (5 * s + 4 * s + 3 * s + 2 * s + s);
end