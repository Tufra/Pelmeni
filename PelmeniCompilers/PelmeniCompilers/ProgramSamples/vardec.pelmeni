module Vardec;

type A is record
    var a: integer;
    var b: real;
end;

type B is record
    var c: A;
    var d: string;
end;

routine ddd(): string is	
	Print("ddd");
	return "mmm";
end


routine empty(): integer is
    IntToString(6);
    	Print("empty");
	return (5 * 1 + 4 * 1 + 3 * 1 + 2 * 1 + 1);
end