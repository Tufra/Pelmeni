module Amogus

use "sum.pelmeni";

routine PrintArray(arr: array[] char) is
  for i in 0..arr.length 
  loop
	  write(arr[i]);
  end
end

routine Main() is
  var arr: array[5] char;
  PrintArray(arr);
end